name: GitHub Actions Build and Deploy Demo
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@master

    - name: Build and Deploy
      uses: delong1998/github-pages-deploy-action@master
      env:
        ACCESS_TOKEN: ${{ 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 }}
        BRANCH: gh-pages
        FOLDER: build
        BUILD_SCRIPT: npm install && npm run build
